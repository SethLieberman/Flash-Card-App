alert("HELOOOOOO SETH: ")
alert("<%= @cards.inspect %>");
alert(gon.watch.cardDisplay);
alert(gon.richard);

// var my_ajax_var = <%= @my_ajax_var.html_safe %>

	// $(".loadButton").submit(function(){
	// 	//use the jQuery .get to make AJAX request
	// 	//to go to the home controller, card#load (custom action)
	// 	//the data received is called data
	// 	$.get("/home/card_load", function(data){
	// 		//set the card question and answer to the returned data to display on page
	// 		//this way the user knows a set of cards was loaded
	// 		$(".cardQuestion").html(data[counter].question);
	// 		$(".cardAnswer").html(data[counter].answer);
	// 		// create a global var cardDisplay within the function for use
	// 		// with the next and previous buttons
	// 		//this var contains the array of cards
	// 		cardDisplay = data
	// 		//console log to check that data is being received
	// 		console.log(data)
	// 	});	
	// 	// console.log(ajax_card_display_update);
	// 	//do I want to put gon.cardDisplay in here so it returns it?
	// });