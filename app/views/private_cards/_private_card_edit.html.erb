	

	<%= form_for card, remote: true, html: {method: :patch}, html: {class: "private-cards"} do |c| %>
				<%= c.label :Question %>
				<%= c.text_field :question %>
				<%= c.label :Answer %>
				<%= c.text_field :answer %>
				<%= c.label :Tag %>
				<%= c.text_field :tag %>
				<%= c.submit :Edit %>
			<% end %>
